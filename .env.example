# CatalogueATC Deployment Configuration
# Copy this file to .env and fill in your credentials
# Usage: source .env && ./deployment.sh

# Ionos FTP/SFTP Credentials
# Get these from: Ionos Control Panel → Web Hosting → FTP Access
IONOS_HOST="ftp.yourdomain.com"           # Your Ionos FTP hostname (e.g., ftp.ionos.eu)
IONOS_USER="your_username"                 # FTP username (usually your domain)
IONOS_PASS="your_secure_password"          # FTP password (keep this SECRET!)

# Ionos FTP/SFTP Port (default: 22 for SFTP, 21 for FTP)
IONOS_PORT="22"                            # 22 for SFTP, 21 for FTP

# Remote deployment path (usually public_html)
REMOTE_PATH="/public_html"                 # Where to upload files

# Your domain (for verification after deployment)
DOMAIN="yourdomain.com"                    # Your production domain

# Protocol choice
USE_SFTP="true"                            # true for SFTP (recommended), false for FTP

# IMPORTANT SECURITY NOTES:
# ============================================================================
# 1. NEVER commit this file to git with real credentials!
# 2. Add .env to .gitignore (already done in most projects)
# 3. Keep this file private and secure
# 4. Use strong passwords (16+ characters, mixed case, numbers, symbols)
# 5. Consider using SSH keys instead of passwords for SFTP:
#    - Generate SSH key: ssh-keygen -t rsa -b 4096
#    - Add public key to Ionos account
#    - Use sftp without password
#
# ALTERNATIVE: Set environment variables directly in terminal:
# ============================================================================
# export IONOS_HOST="ftp.yourdomain.com"
# export IONOS_USER="username"
# export IONOS_PASS="password"
# export DOMAIN="yourdomain.com"
# ./deployment.sh
#
# OR use .env file (more recommended):
# ============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit with your credentials: nano .env
# 3. Load variables: source .env
# 4. Run script: ./deployment.sh
#
# Getting Ionos Credentials:
# ============================================================================
# 1. Login to: https://www.ionos.com/signin
# 2. Go to: Web Hosting → Manage → [Your Domain]
# 3. Select: "FTP Access" or "File Manager"
# 4. Note down:
#    - FTP hostname (e.g., ftp123.ionos.eu or ftp.yourdomain.com)
#    - FTP username
#    - FTP password
#    - Or use SSH/SFTP if available (more secure)
#
# SSL Certificate Setup (after deployment):
# ============================================================================
# 1. Ionos Control Panel → Web Hosting → SSL Certificates
# 2. Click "Activate" on free SSL certificate
# 3. Wait 15-30 minutes for activation
# 4. HTTPS will work automatically via .htaccess redirect
#
# DNS Configuration (if needed):
# ============================================================================
# Option 1: Use Ionos nameservers (recommended)
#   Nameserver 1: ns1.ionos.eu
#   Nameserver 2: ns2.ionos.eu
#
# Option 2: Create A record (if using external DNS)
#   A Record: yourdomain.com → [Ionos IP from Control Panel]
#
# Option 3: Create CNAME record for www
#   CNAME: www → yourdomain.com
#
# Wait 24-48 hours for DNS propagation
#
# Troubleshooting:
# ============================================================================
# "Connection refused": Check hostname, username, password
# "Permission denied": Verify credentials, may need SSH key
# "CORS error": Add your domain to backend CORS headers
# "404 images": Verify backend/ folder uploaded to public_html/backend/
# "DNS not resolving": Wait 24-48 hours for propagation, or check DNS settings
